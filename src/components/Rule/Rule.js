import React from "react";
import styles from "./Rule.module.css";

const Rule = () => {
  return (
    <div className={styles.mainContainer}>
      <hr></hr>
    </div>
  );
};

export default Rule;
